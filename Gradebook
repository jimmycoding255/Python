#organize subjects and grades using Python.

last_semester_gradebook = [["politics", 80], ["latin", 96], ["dance", 97], ["architecture", 65]]

# Your code below: 

#current semester's gradebook

subjects = ["physics", "calculus", "poetry", "history"]

grades = [98, 97, 85, 88]

#two-dimensional list of subjects and grades

gradebook = [
["physics", 98],
["calculus", 97],
["poetry", 85],
["history", 88]
]

print(gradebook)

#append classes and grades

gradebook.append(["computer science", 100])
gradebook.append(["visual arts", 93])

#add an extra 5 points to the visual arts class

gradebook[-1][-1] = 98

#remove the grade in the poetry class

gradebook[2].remove(85)

#append a "Pass" grade to the poetry class
gradebook[2].append("Pass")

#semester grades combined
full_gradebook = last_semester_gradebook + gradebook

print(full_gradebook)
